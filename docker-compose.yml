services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sleuth
    ports:
      - "5433:5432"
    volumes:
      - sleuth_pg_data:/var/lib/postgresql/data

volumes:
  sleuth_pg_data:
