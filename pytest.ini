[pytest]
# scope collection to SLEUTH's own tests only — vendor/ contains third-party
# grammar source (tree-sitter-vue) with its own test_binding.py etc. that must
# never be collected/executed as if they were SLEUTH's tests.
testpaths = tests
